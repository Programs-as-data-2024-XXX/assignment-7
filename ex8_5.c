void main() {
    int value;  // Assign the value 1 to the variable
    value = 1;

    // Use the ternary operator to check if value > 0 and print accordingly
    (value > 0) ? print (value) : print 0;
}
